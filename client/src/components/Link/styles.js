import styled from 'styled-components';

export const Wrapper = styled.a`
  color: #00adb5;
  border-bottom: solid 1px #00adb5;
  padding-bottom: 5px;

  &:hover {
    cursor: pointer;
    width: calc(100% + 10px);
  }
`;
